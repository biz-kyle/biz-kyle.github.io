---
layout: default
---
<div class="post">

{% assign pubtime = "" %}
{% capture pubtime %}{{ page.date }}{% endcapture %}

  <header class="post-header">
    <h1 class="post-title">{{ page.title }}</h1>
	 <!-- Properly format date for display -->
	 <p class="post-meta">{{ page.date | date: "%b %-d, %Y" }}{% if page.author %} • {{ page.author }}{% endif %}{% if page.meta %} • {{ page.meta }}{% endif %} | {{ pubtime | slice: 12, 16 }}</p>
  </header>

  <article class="post-content">
			 <!-- Pull header image -->
			 {% if page.image != null %}
			 	{% include image src=page.image style="topImg" cap=page.cap %}
			 {% endif %}

	 <!-- Article content -->
	 <div class="article-text">
    {{ content }}
	 </div>

	 <!--Tag post with similar content-->
	 <h4>Related posts</h4>
	 	<ul>
				  {% include related_posts.html %}
		</ul>

  </article>

</div>
